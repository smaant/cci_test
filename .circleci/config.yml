version: 2.0
jobs:
  build:
    branches:
      ignore:
        - new-config
    docker:
      - image: alpine:3.5
        cmd: [sh]
        environment:
          SOME_VAR: root

    working_directory: /
    steps:
      - run:
          shell: /bin/sh
          command: echo $SOME_VAR
